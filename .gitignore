# Secrets (per-developer Spotify Client ID) — never commit
Secrets.xcconfig

# XcodeGen output (regenerate with `xcodegen generate`)
*.xcodeproj/

# Build artifacts
build/
DerivedData/
.build/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# Android — SDK path + per-developer Spotify Client ID; never commit
android/local.properties
android/.gradle/
android/.kotlin/
android/app/release/
.idea/
*.iml
